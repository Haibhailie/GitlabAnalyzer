get:
  description: 'logout with deleting an existing cookie'
  responses:
    '200':
      description: deleted an existing cookie successfully.
  parameters:
    - $ref: './components/jwtcookie.yml'
